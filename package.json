{
  "name": "pseudogo-web",
  "private": true,
  "description": "PseudoGo: a browser-based IDE and interpreter for the PseudoGo teaching pseudocode language.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "pnpm -r --if-present run lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
