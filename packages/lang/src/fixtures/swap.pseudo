PROCEDURE swap(IN/OUT a, b: INTEGER)
DICTIONARY
	temp: INTEGER
ALGORITHM
	temp = a
	a = b
	b = temp
ENDPROCEDURE

PROGRAM Main
DICTIONARY
	x, y: INTEGER
ALGORITHM
	x = 10
	y = 20
	swap(x, y)
	OUTPUT x, y
ENDPROGRAM
